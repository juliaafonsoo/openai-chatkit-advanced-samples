# Backend environment variables for MEDICALS Gmail Agent

# Required: OpenAI API key used by the agents library
OPENAI_API_KEY="sk-proj-REPLACE_ME"

# Required: CEL expression that evaluates to the Gmail OAuth bearer token.
# Keep the inner token quoted so the expression is a CEL string literal.
# Example below passes the raw token string to the connector.
GMAIL_MCP_AUTH_EXPRESSION="\"ya29.REPLACE_ME\""

# Optional: Uvicorn port (defaults to 8000 if not set)
# PORT=8000

