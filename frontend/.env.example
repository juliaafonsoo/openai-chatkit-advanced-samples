# Frontend environment variables for ChatKit demo UI

# Domain key used by ChatKit SDK. Use any non-empty placeholder locally.
VITE_CHATKIT_API_DOMAIN_KEY=domain_pk_local_dev

# API routes proxied by Vite to the backend.
# Defaults match vite.config.ts. Adjust BACKEND_URL if your backend runs elsewhere.
VITE_CHATKIT_API_URL=/chatkit
VITE_FACTS_API_URL=/facts

# Vite dev server proxy target for backend (used in vite.config.ts)
BACKEND_URL=http://127.0.0.1:8000

